import { PricingMethodFilterPipe } from './pricing-method-filter.pipe';

describe('PricingMethodFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PricingMethodFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
